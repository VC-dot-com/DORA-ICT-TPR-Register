# Application source
The Flask application lives here: application factory, database models, the concentration-risk scoring engine,
routes, templates, and the register export. Code is added from Unit 4 onward.
